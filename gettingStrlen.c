#include "shell.h"
/**
 * gettingStrlen -calculate length of a given string
 *@s:  is a pointer point to the address of current index
 *Return: Always 0.
 */
int gettingStrlens(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
continue;
}
return (i);
}
